<!DOCTYPE html>
  <% if (user) { %>
  <p> You are logged in as <strong> <%= user.email %> </strong>
    <form action="/logout" method="POST" >
      <input type="submit" value="Log out"></p>
    </form>
  <% } else { %>

  <a href="/login">Login</a>
  <a href="/register">Register</a>

<% } %>
